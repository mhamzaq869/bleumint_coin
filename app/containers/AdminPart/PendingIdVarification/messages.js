/*
 * PendingIdVarification Messages
 *
 * This contains all the text for the PendingIdVarification container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PendingIdVarification';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PendingIdVarification container!',
  },
});
